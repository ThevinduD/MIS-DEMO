@model MIS_DEMO.Models.ViewModels.DeliveredInvoicesViewModel

@{
    ViewData["Title"] = "Delivered Invoices";

    decimal ToK(decimal amt) => amt / 1000m;
    string KText(decimal amt) => $"{Math.Round(ToK(amt), 1):0.0}K";
}

<div class="d-flex justify-content-between align-items-start mb-2">
    <div>
        <h4 class="fw-bold mb-0">@ViewBag.Title</h4>
        <small class="text-muted">@ViewBag.SubTitle</small>
    </div>
    <div class="text-end">
        <a class="btn btn-sm btn-outline-secondary mt-1" href="javascript:history.back()">Back</a>
    </div>
</div>

<div class="card shadow-sm mt-3">
    <div class="card-body p-2">
        <div class="table-responsive">
            <table class="table table-bordered table-sm text-center inv-summary-table">
                <thead class="table-light">
                    <tr class="text-center">
                        <th style="min-width:80px;">TEAM</th>
                        <th style="min-width:90px;">No of Inv</th>
                        <th style="min-width:120px;">Amount</th>
                    </tr>
                </thead>

                <tbody>
                    @if (Model.Rows == null || !Model.Rows.Any())
                    {
                        <tr>
                            <td colspan="3" class="text-center text-muted py-3">
                                No delivered invoices found for this month.
                            </td>
                        </tr>
                    }
                    else
                    {
                        foreach (var r in Model.Rows)
                        {
                            <tr>
                                <td class="fw-semibold text-start">@r.Team</td>
                                <td class="text-end">
                                    <a class="fw-semibold"
                                       asp-controller="Delivered"
                                       asp-action="TeamInvoices"
                                       asp-route-team="@r.Team">
                                        @r.DeliveredCount
                                    </a>
                                </td>
                                <td class="text-end">@KText(r.DeliveredAmount)</td>
                            </tr>
                        }

                        <tr class="table-warning fw-bold">
                            <td style="background-color:#343a40" class="text-white text-start">Total</td>
                            <td class="text-end bg-warning-subtle">@Model.DeliveredCountTotal</td>
                            <td class="text-end bg-warning-subtle">@KText(Model.DeliveredAmountTotal)</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <div class="small text-muted mt-2">
            Amounts shown in K (1K = 1,000 LKR)
        </div>
    </div>
</div>